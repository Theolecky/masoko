<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.masoko">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/splash_screen"
        android:label="@string/app_name"
        android:roundIcon="@drawable/splash_screen"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AddStockActivity"></activity>
        <activity android:name=".AddProductActivity" />
        <activity android:name=".AddShopDetails" /> <!-- <activity android:name=".SplashActivity"></activity> -->
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UserOptionsActivity" />
        <activity android:name=".SellerLogActivity" />
        <activity android:name=".CustomerLogActivity" />
        <activity
            android:name=".SellerMainActivity"
            android:label="@string/title_activity_parent_map" />
    </application>

</manifest>